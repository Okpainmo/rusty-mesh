#!/usr/bin/env sh

info() {
  printf '%s\n' "$1"
}

info "🧪 Pre-push: running test suite..."
cargo test || exit 1

info "📝 Checking markdown formatting..."
bunx prettier --check "**/*.md" || exit 1

info "✅ Pre-push: all checks passed. Push approved."
